version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: cool_galois
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Docker@1
    ports:
      - "1433:1433"
    restart: unless-stopped
